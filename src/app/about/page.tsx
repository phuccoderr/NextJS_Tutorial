import { Metadata } from "next";
import React from "react";

export const metadata = {
  title: "About me",
};

const AboutPage = () => {
  // throw new Error("not working");
  return <div>Abount me</div>;
};

export default AboutPage;
